Requirements for project
------------------------
- JDK 8
- Gradle v. 1.9 or newer (optional, you can also just use gradlew (*nix) or gradlew.bat (win)


Building project
------------------------
- "gradle build", or "gradlew build" if using gradle wrapper.
